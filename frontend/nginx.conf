server { listen 80; root /usr/share/nginx/html; index index.html; client_max_body_size 100m;
  location /api/ { proxy_pass http://backend:8080/api/; proxy_http_version 1.1; }
  location / { try_files $uri /index.html; }
}

